[toolchain]
# 1.91.1 minimum: keeps the AWS SDK TLS dependency chain on a cargo-deny-clean line.
# Previous floor: 1.88.0 for sqlx 0.8 + rustls transitive dependency requirements.
channel = "1.91.1"
components = ["rustfmt", "clippy", "rust-analyzer", "rust-src"]
profile = "default"
