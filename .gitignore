# === Env ===
.env
.env.local
.env.*.local
!.env.example

# === Node / pnpm / TS ===
node_modules/
.pnpm-store/
.turbo/
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo
test-results/
playwright-report/

# === Rust / Cargo ===
target/
*.rs.bk
Cargo.lock.bak

# === Logs ===
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === OS ===
.DS_Store
Thumbs.db

# === IDE ===
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# === Claude / 로컬 메모리 ===
.claude/settings.local.json
.claude/*.local.*
CLAUDE.local.md
AGENTS.local.md

# === 빌드 산출물 / 임시 ===
*.tgz
*.zip

# === ETL 산출물 (SP9) ===
# Bronze 다운로드 / Gold PMTiles 빌드 결과 — R2 업로드 전 임시.
var/
# Next.js public 에 복사되는 PMTiles (재생성 가능, R2 우선).
apps/web/public/pmtiles/
.cache/
.temp/
.tmp/

# === 학습용 외부 레포 (빌드 제외) ===
reference/*/
!reference/README.md

# === 로컬 MCP 빌드 ===
.mcp-local/

# === Dev verification scripts (local 검증, CI 무관) ===
apps/web/scripts/

# === 백업 / 아카이브 ===
_archive/
.claude/scheduled_tasks.lock

