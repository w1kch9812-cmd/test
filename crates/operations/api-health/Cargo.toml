[package]
name = "api-health-domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
description = "공짱 API Health domain — SP7-iii 정부 API drift 자동 검출 SSOT (HealthStatus / HealthCheckRecord / HealthCheckRepository port)"

[dependencies]
chrono = { workspace = true }
serde = { workspace = true, features = ["derive"] }
async-trait = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
