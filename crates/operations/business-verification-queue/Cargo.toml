[package]
name = "business-verification-queue-domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license = "Apache-2.0"
description = "공짱 BusinessVerificationQueue domain (Operations BC, RDS 동적 — 4-status workflow + version OCC + 24h SLA)"

[dependencies]
shared-kernel = { path = "../../domain/core/shared-kernel", version = "0.1.0" }
chrono = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
async-trait = { workspace = true }

[lints]
workspace = true
