{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalDependencies": ["**/.env", "**/.env.local", "biome.json", "tsconfig.base.json"],
  "globalEnv": [
    "NODE_ENV",
    "VWORLD_API_KEY",
    "VWORLD_DOMAIN",
    "KOREAN_LAW_API_KEY",
    "ODP_SERVICE_KEY",
    "NICE_API_KEY",
    "NICE_CLIENT_ID",
    "NAVER_MAPS_CLIENT_ID",
    "NAVER_MAPS_CLIENT_SECRET",
    "ZITADEL_DOMAIN",
    "ZITADEL_PROJECT_ID",
    "ZITADEL_API_KEY",
    "ZITADEL_ISSUER",
    "ZITADEL_CLIENT_ID",
    "ZITADEL_AUDIENCE",
    "ZITADEL_REDIRECT_URI",
    "SESSION_SECRET",
    "INTERNAL_AUTH_SECRET",
    "NEXT_PUBLIC_API_BASE_URL",
    "NEXT_PUBLIC_PLATFORM_CORE_BASE_URL",
    "NEXT_PUBLIC_TILES_MANIFEST_URL",
    "GEMINI_API_KEY",
    "DATABASE_URL",
    "REDIS_URL",
    "SENTRY_DSN",
    "OTEL_EXPORTER_OTLP_ENDPOINT",
    "OTEL_SERVICE_NAME",
    "NEXT_PUBLIC_NAVER_MAPS_CLIENT_ID"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": []
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": []
    },
    "test:e2e": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$"],
      "outputs": ["playwright-report/**", "test-results/**"]
    },
    "lint": {
      "inputs": ["$TURBO_DEFAULT$"]
    },
    "clean": {
      "cache": false
    }
  }
}
